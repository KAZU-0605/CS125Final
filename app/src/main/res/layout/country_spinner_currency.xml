<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageView
        android:id="@+id/country_flag_image"
        android:layout_width="50dp"
        android:layout_height="40dp"
        android:src="@drawable/usa" />

    <TextView
        android:id="@+id/country_flag_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/country_flag_image"
        android:layout_alignParentTop="true"
        android:layout_marginStart="-36dp"
        android:layout_marginLeft="-36dp"
        android:layout_marginTop="35dp"
        android:layout_marginEnd="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="-25dp"
        android:layout_toEndOf="@id/country_flag_image"
        android:layout_toRightOf="@id/country_flag_image"
        android:gravity="center"
        android:text="USD"
        android:textColor="@android:color/black"
        android:textSize="10sp" />
</RelativeLayout>